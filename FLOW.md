# 📊 Fluxo da Aplicação - Gerenciador de Tarefas

Documentação detalhada do fluxo de funcionamento do aplicativo.

---

## 🔄 Fluxo Geral da Aplicação

```
┌─────────────────────────────────────────────────────────────┐
│         INICIALIZAÇÃO DA APLICAÇÃO                           │
└────────────────────┬────────────────────────────────────────┘
                     │
        ┌────────────▼────────────┐
        │   loadTasks()           │
        │ Carrega do localStorage │
        └────────────┬────────────┘
                     │
        ┌────────────▼────────────┐
        │   renderTasks()         │
        │ Renderiza lista inicial │
        └────────────┬────────────┘
                     │
        ┌────────────▼────────────────────────┐
        │ Aplicação Pronta para Interação     │
        └─────────────────────────────────────┘
```

---

## 1️⃣ Criação de Nova Tarefa

```
┌──────────────────────────────────────┐
│ Usuário preenche o formulário        │
│ - Nome da tarefa                      │
│ - Etiqueta/Categoria                  │
└─────────────┬──────────────────────────┘
              │
    ┌─────────▼─────────┐
    │ Evento "submit"   │
    │ ativado           │
    └─────────┬─────────┘
              │
    ┌─────────▼──────────────────────┐
    │ Validação                       │
    │ - Nome não vazio?               │
    │ - Etiqueta não vazio?           │
    └─────────┬──────────────────────┘
              │
         [Inválido?]
         /          \
       SIM           NÃO
        │             │
        ▼             │
    Alert do        │
    erro            │
        │           │
        └───────┬───┘
                │
    ┌───────────▼──────────────────────┐
    │ Cria objeto tarefa:              │
    │ {                                │
    │   name: "...",                   │
    │   label: "...",                  │
    │   date: "dd/mm/yyyy",            │
    │   dateISO: "yyyy-mm-dd",         │
    │   completed: false               │
    │ }                                │
    └────────────┬─────────────────────┘
                 │
    ┌────────────▼────────────┐
    │ Adiciona ao array       │
    │ tasks.push(newTask)     │
    └────────────┬────────────┘
                 │
    ┌────────────▼────────────┐
    │ Limpa campos input      │
    └────────────┬────────────┘
                 │
    ┌────────────▼────────────────────────────────┐
    │ renderTasks()                               │
    │ - Atualiza visualização                     │
    │ - Atualiza contador                         │
    │ - Salva no localStorage                     │
    └─────────────────────────────────────────────┘
```

---

## 2️⃣ Renderização da Lista

```
┌────────────────────────────────────────┐
│ renderTasks() chamada                  │
└────────────┬─────────────────────────────┘
             │
    ┌────────▼─────────────────┐
    │ Limpa a lista de tarefas │
    │ (remove nós filhos)      │
    └────────┬──────────────────┘
             │
    ┌────────▼──────────────────────┐
    │ Para cada tarefa no array:    │
    │                               │
    │ createTaskElement(task, index)│
    │                               │
    │ └─> Cria: <article>           │
    │     ├─ Conteúdo da tarefa    │
    │     ├─ Botão "Concluir"      │
    │     └─ Botão "Deletar"       │
    │                               │
    │ → Adiciona ao DOM            │
    └────────┬──────────────────────┘
             │
    ┌────────▼──────────────────┐
    │ updateFooter()            │
    │                           │
    │ - Conta completadas       │
    │ - Atualiza texto footer   │
    │ - Mostra/esconde botão    │
    │   "Deletar Tudo"          │
    └────────┬──────────────────┘
             │
    ┌────────▼──────────────────┐
    │ saveTasks()               │
    │                           │
    │ localStorage.setItem(     │
    │   "tasks",                │
    │   JSON.stringify(tasks)   │
    │ )                         │
    └───────────────────────────┘
```

---

## 3️⃣ Completar/Desconcluir Tarefa

```
┌──────────────────────────────────┐
│ Clica no botão "Concluir" ou ✓   │
└────────────┬─────────────────────┘
             │
    ┌────────▼────────────────┐
    │ Event listener ativado  │
    └────────┬────────────────┘
             │
    ┌────────▼──────────────────────┐
    │ Alterna propriedade:          │
    │ tasks[index].completed =      │
    │   !tasks[index].completed     │
    └────────┬─────────────────────┘
             │
    ┌────────▼────────────────┐
    │ renderTasks()           │
    │ (atualiza visual)       │
    └────────────────────────┘

Resultado na UI:
├─ Se completed = false
│  └─ Botão "Concluir" visível
│
└─ Se completed = true
   ├─ Botão substituído por ✓
   ├─ Texto da tarefa riscado
   └─ Card com classe "completed"
```

---

## 4️⃣ Deletar Tarefa

```
┌──────────────────────────┐
│ Clica no botão 🗑️        │
└────────────┬─────────────┘
             │
    ┌────────▼──────────────────────┐
    │ Popup de confirmação           │
    │ "Tem certeza que deseja        │
    │  deletar esta tarefa?"         │
    └────────┬─────────────────────┘
             │
        [Resposta?]
        /          \
    Cancelar      Confirmar
      │              │
      ▼              ▼
     Nada       deleteTask(index)
              │
    ┌─────────▼──────────────────┐
    │ Remove do array:           │
    │ tasks.splice(index, 1)     │
    └─────────┬──────────────────┘
              │
    ┌─────────▼──────────────────┐
    │ renderTasks()              │
    │ (atualiza lista)           │
    └────────────────────────────┘
```

---

## 5️⃣ Deletar Todas as Tarefas

```
┌─────────────────────────────┐
│ Clica em "Deletar Tudo"     │
└────────────┬────────────────┘
             │
    ┌────────▼──────────────────────────────┐
    │ Verifica se há tarefas                │
    │ if (tasks.length === 0) return        │
    └────────┬─────────────────────────────┘
             │
    ┌────────▼──────────────────────┐
    │ Popup de confirmação:         │
    │ "Tem certeza que deseja       │
    │  deletar TODAS? Esta ação    │
    │  não pode ser desfeita."     │
    └────────┬──────────────────────┘
             │
        [Resposta?]
        /          \
    Cancelar      Confirmar
      │              │
      ▼              ▼
     Nada      deleteAllTasks()
              │
    ┌─────────▼────────────────┐
    │ Limpa array:             │
    │ tasks = []               │
    └─────────┬────────────────┘
              │
    ┌─────────▼──────────────────┐
    │ renderTasks()              │
    │ (atualiza lista vazia)     │
    └────────────────────────────┘
```

---

## 📊 Estrutura de Dados

### Array `tasks`

```javascript
tasks = [
  {
    name: "Estudar JavaScript", // String
    label: "Educação", // String
    date: "19/12/2025", // String (PT-BR)
    dateISO: "2025-12-19", // String (ISO)
    completed: false, // Boolean
  },
  {
    name: "Fazer exercício",
    label: "Saúde",
    date: "19/12/2025",
    dateISO: "2025-12-19",
    completed: true,
  },
  // ... mais tarefas
];
```

### Estado da Tarefa

```
┌─────────────────────────────────┐
│ TAREFA NÃO CONCLUÍDA            │
├─────────────────────────────────┤
│ completed: false                │
│ Botão: "Concluir"               │
│ Texto: Normal                   │
│ Ícone: Nenhum (o botão é texto) │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ TAREFA CONCLUÍDA                │
├─────────────────────────────────┤
│ completed: true                 │
│ Botão: ✓ (checked.svg)          │
│ Texto: Riscado (strikethrough)  │
│ Card: Adiciona classe "completed"│
└─────────────────────────────────┘
```

---

## 💾 Ciclo de Persistência

```
┌────────────────────────────────────────────┐
│ ABERTURA DO NAVEGADOR                      │
└───────────────┬────────────────────────────┘
                │
    ┌───────────▼──────────────┐
    │ loadTasks()              │
    │                          │
    │ localStorage.getItem()   │
    │ → JSON.parse()           │
    └───────────┬──────────────┘
                │
    ┌───────────▼──────────────────────────┐
    │ renderTasks()                        │
    │ (exibe tarefas salvas)               │
    └────────────────────────────────────┘

                    ↓ [Ações do usuário]

    ┌───────────────────────────────────────┐
    │ QUALQUER MUDANÇA NA LISTA             │
    │ - Adicionar tarefa                    │
    │ - Marcar como concluída               │
    │ - Deletar tarefa                      │
    │ - Deletar todas                       │
    └───────────┬─────────────────────────┘
                │
    ┌───────────▼──────────────┐
    │ renderTasks() chamado    │
    └───────────┬──────────────┘
                │
    ┌───────────▼──────────────┐
    │ saveTasks()              │
    │                          │
    │ localStorage.setItem()   │
    │ → JSON.stringify()       │
    └───────────┬──────────────┘
                │
    ┌───────────▼──────────────────────────┐
    │ Dados persistidos no navegador       │
    │ (sobrevive ao reload)                │
    └──────────────────────────────────────┘
```

---

## 🎯 Fluxo de Eventos (Resumido)

```
submit form
    ↓
validação
    ↓
criar objeto tarefa
    ↓
adicionar ao array
    ↓
renderTasks()
    ├─ createTaskElement() para cada tarefa
    ├─ adicionar listeners (click no botão)
    ├─ updateFooter()
    └─ saveTasks()

click no botão "Concluir"
    ↓
alternar completed
    ↓
renderTasks()

click no botão "Deletar"
    ↓
confirmar (popup)
    ↓
remover do array
    ↓
renderTasks()

click "Deletar Tudo"
    ↓
confirmar (popup)
    ↓
limpar array
    ↓
renderTasks()
```

---

## 🔑 Funções e Responsabilidades

| Função                | Faz O Quê                        | Chama                           |
| --------------------- | -------------------------------- | ------------------------------- |
| `loadTasks()`         | Carrega do localStorage          | `JSON.parse()`                  |
| `saveTasks()`         | Salva no localStorage            | `JSON.stringify()`              |
| `updateFooter()`      | Conta e exibe tarefas concluídas | Filtra array                    |
| `createTaskElement()` | Cria HTML de um card             | `addEventListener()`            |
| `renderTasks()`       | Renderiza tudo + salva           | `updateFooter()`, `saveTasks()` |
| `deleteTask()`        | Remove uma tarefa                | `renderTasks()`                 |
| `deleteAllTasks()`    | Remove todas com confirmação     | `renderTasks()`                 |

---

## 📋 Estados e Transições

```
         ┌──────────────┐
         │ Nova Tarefa  │
         │ (não vista)  │
         └──────┬───────┘
                │ adicionada ao array
                ▼
         ┌──────────────────┐
         │ Tarefa Exibida   │
         │ completed=false  │
         └──────┬───────────┘
              / │ \
            /   │   \
    Deletar │   │    │ Concluir
          │   │    │
          ▼   ▼    ▼
         Deletada  ┌───────────────┐
                   │ Tarefa        │
                   │ Concluída     │
                   │ completed=true│
                   └───┬───────────┘
                       │ Deletar
                       ▼
                    Deletada
```
